---
title: Installation
---

# Installation

Dependabit is distributed as a set of scoped packages under `@dependabit/*`.

```bash
# Core runtime
pnpm add @dependabit/core

# Or the CLI / GitHub Action bundle
pnpm add -D @dependabit/action
```

## Requirements

- Node.js >= 20
- pnpm >= 10 (for contributing)

## GitHub Action

See [Auto-Update](./auto-update) for wiring Dependabit into a GitHub Action workflow.
